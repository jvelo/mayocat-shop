UI :
----

* Admin:
  - pagination on product images
  - finish edit thumbnail dialog
  - display correct thumnail on image set show screen
* Storefront:
  - pagination on collection products

Backend :
---------

- Lazily create thumbnails when they are requested at a specific size that does not exist
- Remove the EschoppeThymeleafResourceResolver.java as we don't need it anymore
- Add modification header when serving resources via ResourceController
